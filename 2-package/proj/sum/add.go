package sum

//all files in a package must belong to one package only

// function first letter must be uppercase to export the function
func Add(a, b int) int {
	return a + b
}

func Sum(a, b int) int {
	return divide(a, b)
}
